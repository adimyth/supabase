table:
  schema: launchpad
  name: role
array_relationships:
  - name: role_has_modules
    using:
      foreign_key_constraint_on:
        column: role_id_role
        table:
          schema: launchpad
          name: role_has_module
